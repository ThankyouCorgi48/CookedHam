package org.aguerra.cookedham.compile;

public enum Type {
    INTEGER_LITERAL,
    DECIMAL_LITERAL,
    CHARACTER_LITERAL,
    STRING_LITERAL,
    BOOL_LITERAL,
    IDENTIFIER,
    ASSIGNMENT,
    OPERATOR,
    KEYWORD,
    UNKNOWN
}